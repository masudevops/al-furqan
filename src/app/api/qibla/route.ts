import { NextRequest,NextResponse } from "next/server";
export const dynamic="force-dynamic";
export async function GET(request:NextRequest){const lat=Number(request.nextUrl.searchParams.get("latitude"));const lon=Number(request.nextUrl.searchParams.get("longitude"));if(!Number.isFinite(lat)||!Number.isFinite(lon)||Math.abs(lat)>90||Math.abs(lon)>180)return NextResponse.json({error:"Invalid coordinates."},{status:400});try{const response=await fetch(`https://api.aladhan.com/v1/qibla/${lat}/${lon}`,{next:{revalidate:86_400}});const payload=await response.json();const direction=Number(payload?.data?.direction);if(!response.ok||!Number.isFinite(direction))throw new Error();return NextResponse.json({direction,error:null})}catch{return NextResponse.json({error:"Qibla direction is unavailable right now."},{status:502})}}
