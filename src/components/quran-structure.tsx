"use client";
import Link from "next/link";
import useSWR from "swr";
import styles from "./quran-tools.module.css";
const fetcher=(url:string)=>fetch(url).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error);return d});
export function QuranStructureIndex(){const groups=[{kind:"juz",label:"Juz",count:30,description:"Thirty equal reading portions."},{kind:"hizb",label:"Hizb",count:60,description:"Sixty half-Juz divisions."},{kind:"rub",label:"Rub el Hizb",count:240,description:"Quarter-Hizb reading markers."}];return <main className={styles.page}><header className={styles.header}><div><Link href="/quran">← Quran</Link><h1>Browse by structure</h1><p>Navigate the Quran using authoritative structural metadata from Quran.Foundation.</p></div><div className={styles.tools}><Link href="/quran/mushaf/1">Open Mushaf view →</Link></div></header><section className={styles.structureGrid}>{groups.map(group=><article className={styles.structureCard} key={group.kind}><h2>{group.label}</h2><p>{group.description}</p><div className={styles.numberGrid}>{Array.from({length:group.count},(_,i)=><Link href={`/quran/structure/${group.kind}/${i+1}`} key={i+1}>{i+1}</Link>)}</div></article>)}</section></main>}
export function QuranStructureReader({kind,id}:{kind:string;id:number}){const valid=kind==="juz"||kind==="hizb"||kind==="rub";const{data,error,isLoading}=useSWR<{items:Array<{arabicText:string;verseKey:string;pageNumber:number}>}>(valid?`/api/quran/structure?kind=${kind}&id=${id}`:null,fetcher);const label=kind==="rub"?"Rub el Hizb":kind[0]?.toUpperCase()+kind.slice(1);return <main className={styles.page}><header className={styles.header}><div><Link href="/quran/structure">← Quran structure</Link><h1>{label} {id}</h1><p>Quran text is provided directly by Quran.Foundation.</p></div></header>{isLoading?<p>Loading Quran verses…</p>:null}{error?<p className={styles.error}>This Quran section is unavailable right now.</p>:null}<section className={styles.verseList}>{data?.items.map(item=><article className={styles.verse} key={item.verseKey}><Link href={`/quran/${item.verseKey.replace(":","/")}`}>{item.verseKey} · Mushaf page {item.pageNumber}</Link><p lang="ar" dir="rtl" translate="no">{item.arabicText}</p></article>)}</section></main>}
