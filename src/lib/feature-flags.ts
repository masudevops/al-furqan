// Keep Early Access Hadith opt-in until the production key and provider
// responses have been verified for the deployed environment.
// No Hadith provider currently satisfies the app's coverage and provenance gate.
export const HADITH_ENABLED = false;
